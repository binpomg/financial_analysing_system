# 验证范围与历史摘要

程序可运行、接口可调用和金融研究可靠性是不同结论。公开仓库包含源码、方法与离线测试，不附私人运行trace、认证配置、接口地址、下载原件或历史运行ID。

## 程序检查

本次发布在Windows、Python 3.12.14下完成 **287项离线测试，0失败、0错误、0跳过**，包括原有282项及新增5项发布安全检查。测试过程中阻断外部网络连接且未配置模型认证，真实模型调用为0；另外验证了锁定依赖的全新安装、空资料库、七条业务线加载和工作台HTTP启动。完整checkout安装锁定依赖后，可自行运行：

```text
python -m unittest discover -s test -v
python -m finresearch doctor
```

不带 `--live` 的检查无需密钥，不会请求模型。测试使用隔离临时资料库、模拟Provider和网络响应，涵盖页面准备、来源边界、引用、时点、分区、接口失败、精确计算、审核语义、队列、有限批次、中断恢复和候选保护。当前checkout的实际测试数量和结果以命令输出为准。

模拟响应证明所测试的程序行为，不能证明实际模型能正确读懂金融材料。GitHub Actions对Windows与Ubuntu分别安装锁定依赖，执行完整离线测试、离线诊断以及工作区和Git全历史的发布检查；各提交的跨平台实测结果以仓库Actions为准。

## 历史10份材料运行

2026年9月开展过一次公开公告开发批次：5份中标公告、5份经营数据公告，逐份进入七条业务线，共70次业务尝试。来源限定为巨潮两类目录，查询窗口固定为2026年6月17日至9月15日；这些短公告不能代表复杂财报、长研报或所有投资任务。

目录整理配置为 `gpt-5.6-luna / xhigh`，业务、独立审核及核实配置为 `gpt-6-astra / xhigh`。下表仅为历史记录摘要，公开仓库不附对应私有trace或原件，读者不能据此独立复核每一项模型判断。

| 业务线 | 尝试 | 完成审核 | 有效通过 | 需修改 | 证据不足 | 失败 | 中断 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 数据结构化提取 | 10 | 7 | 1 | 6 | 0 | 2 | 1 |
| 财务报告分析 | 10 | 6 | 4 | 2 | 0 | 3 | 1 |
| 产业链影响分析 | 10 | 8 | 6 | 2 | 0 | 2 | 0 |
| 估值建模 | 10 | 7 | 0 | 0 | 7 | 3 | 0 |
| 研报事实核查 | 10 | 9 | 8 | 1 | 0 | 1 | 0 |
| 投资备忘录 | 10 | 6 | 6 | 0 | 0 | 3 | 1 |
| 研究质量评估 | 10 | 5 | 5 | 0 | 0 | 5 | 0 |
| **合计** | **70** | **48** | **30** | **11** | **7** | **19** | **3** |

有效通过同时要求运行终态为 `audited`、审核裁决为 `pass`。另有一次模型输出了 `pass`，但结论与问题／维度矛盾，程序将该运行判为失败，未计入30次有效通过。

30次有效通过最终为28次 `needs_data`、1次 `not_applicable`、1次 `audit_passed`。指出输入不足可以满足局部任务要求，但不是完整财报、估值或投资结论已经完成。其余18次需复核、22次失败或中断均保留。

19次失败包括14次接口、网络、流式或解码异常，以及5次证据／审核语义校验拦截；3次中断来自执行进程结束。历史记录没有通过删除失败或反复重跑挑选通过结果。

## 反馈与改进结论

该批次积累 **13条完成独立模型核实的反馈**：提取线7条、财报线3条、产业线3条。它们未经专家校准，问题条数也不等于独立样本数。

**新增候选0份、稳定版晋升0次。** 已存在候选的业务线等待独立验证，其余线未达到当前候选积累条件。没有进行该批次的新旧版本对照与历史回归，不能声称稳定Skill已改善。

模型输出和审核具有不确定性，服务版本、接口与输入变化也会影响结果。依赖锁和源码快照有助于复现程序环境，不能保证重新调用得到相同答案、通过数或费用。公开使用者应保留自己的失败、输入与评价记录，并用独立原件核查建立适合其研究任务的验收依据。
